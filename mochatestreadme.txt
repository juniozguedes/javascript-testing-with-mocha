Before we start testing, we need to setup the enviroment

Make sure you have NodeJs installed.

Open the cmd inside desirable folder and type:

npm init

then, to install the mocha framework:

npm install mocha chai --save-dev


===

Create a folder and name it test, put your fileTest.js inside.

Check the files in this folder to study
